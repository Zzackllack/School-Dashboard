{
  "name": "school-dashboard",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "format": "pnpm --dir Frontend run format && mvn -f Backend/pom.xml spotless:apply",
    "format:check": "pnpm --dir Frontend run format:check && mvn -f Backend/pom.xml spotless:check",
    "lint": "pnpm --dir Frontend run lint",
    "build": "pnpm --dir Frontend run build && mvn -f Backend/pom.xml -DskipTests package",
    "test": "mvn -f Backend/pom.xml test"
  }
}
