security.admin.bootstrap.enabled=true
security.admin.bootstrap.username=${SECURITY_ADMIN_BOOTSTRAP_USERNAME:dev-admin}
security.admin.bootstrap.password=${SECURITY_ADMIN_BOOTSTRAP_PASSWORD:}

# Local frontends
security.cors.allowed-origins=http://localhost:3000,http://localhost:5173

# Local development typically uses http
server.servlet.session.cookie.secure=false
